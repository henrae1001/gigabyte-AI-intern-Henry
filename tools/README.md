# Tools

此資料夾可放本機下載的 `llama.cpp` binary，例如 `llama-cli.exe` 或 `llama-server.exe`。

大型 binary 與 zip 檔會被 `.gitignore` 忽略，不會推上 GitHub。

若 `llama-cpp-python` 在特定 Windows / CPU 環境出現 instruction set 不相容，可改用官方 `llama.cpp` release binary 進行本機測試。
